
#include <stdio.h>
struct Point {
	int x;
	int y;
};
struct Node {
	int data;
	struct Node* next;
};
typedef struct {
	char name[10];
	int age;
}Person;

int main()
{	
	struct Point p1 = { 10,20 };
	struct Point* ptr = &p1;

	// 둘다 같은 의미
	//printf("%d \n", ptr->x);
	//printf("%d \n", (*ptr).y);

	struct Node a = { 10,NULL };
	struct Node b = { 20,NULL };
	struct Node c = { 30,NULL };
	a.next = &b;
	b.next = &c;
	c.next = &a;
	//printf("%d \n", a.next->next->next->data);

	Person p[] = { {"Kim",20},{"Lee",30},{"Park",40} };
	Person* pperson = p;
	pperson++;
	printf("%s \n", pperson->name);
}

